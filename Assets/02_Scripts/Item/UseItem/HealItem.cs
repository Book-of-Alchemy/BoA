using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealItem : BaseItem
{
    public override void UseItem(ItemData data)
    {
            //// 공격 로직
            //if (targets.Count < 1)
            //{
            //    Debug.Log("타겟이 없습니다.");
            //    return;
            //}
            //foreach (var target in targets)
            //    target.Heal((float)effect_value);
    }

}
